import { Product } from '@/types';

// Todos os produtos são gerenciados via painel admin (Firestore).
// Este array fica vazio — não há mais produtos hardcoded no código.
export const products: Product[] = [];
