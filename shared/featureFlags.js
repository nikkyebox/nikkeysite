// Flags compartilhadas entre client (src/) e servidor (api/_lib) — mesmo
// motivo de shared/points.js e shared/company-profile.json: manter em dois
// lugares diverge (a tela promete uma coisa, o servidor credita outra).
export const LOYALTY_POINTS_ENABLED = false;
